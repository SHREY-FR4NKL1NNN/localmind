# Public URL of the LocalMind backend the frontend should call.
# In production set this to your backend's public URL; for local dev you can
# omit it entirely and the app falls back to http://127.0.0.1:8000.
VITE_API_URL=https://your-ngrok-url.ngrok-free.app
# Get this URL by running: ngrok http 8000
