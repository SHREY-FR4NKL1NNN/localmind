# Public URL of the LocalMind backend the frontend should call.
# In production set this to the backend's public URL (the Azure Container Apps
# deployment). For local dev you can omit it entirely and the app falls back to
# http://127.0.0.1:8000.
VITE_API_URL=https://localmind-api.happystone-218aa158.centralindia.azurecontainerapps.io
