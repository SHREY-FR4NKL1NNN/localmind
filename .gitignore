# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env files — never commit real secrets/URLs; templates are the exception.
.env
.env.*
!.env.example

# Node / frontend
node_modules/
dist/
*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local-only migration runbook (transitional scaffolding; the durable narrative
# lives in the README "Deployment & Architecture" section, not in git).
MIGRATION.md
